<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Cross-cutting primitives only (TenantId, UserId, DataScopeSet): a contract read takes the caller's
         scope set as an explicit parameter, the convention every module service follows. No module
         project is ever referenced from here (ARCHITECTURE.md §1). -->
    <ProjectReference Include="..\Aperture.SharedKernel\Aperture.SharedKernel.csproj" />
  </ItemGroup>

</Project>
