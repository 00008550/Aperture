<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Aperture.SharedKernel\Aperture.SharedKernel.csproj" />
    <ProjectReference Include="..\Aperture.Contracts\Aperture.Contracts.csproj" />
    <ProjectReference Include="..\Modules\Access\Aperture.Modules.Access\Aperture.Modules.Access.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- The test assembly hosts this API with WebApplicationFactory and asserts against its
         endpoint metadata; nothing else may see internals. -->
    <InternalsVisibleTo Include="Aperture.Api.Tests" />
  </ItemGroup>

</Project>
